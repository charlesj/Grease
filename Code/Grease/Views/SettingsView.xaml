<UserControl x:Class="Grease.Views.SettingsView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 mc:Ignorable="d" 
			 d:DesignHeight="300" d:DesignWidth="300">
	<Grid>
		<StackPanel>
			<Label>Settings</Label>
			<Label>Path to Music Library</Label>
			<TextBox IsReadOnly="True" Text="{Binding RootPath}" />
			<Button Command="{Binding ChangeDirectoryCommand}" Content="Change Directory"></Button>
			<Button Command="{Binding GoBackCommand}" Content="Go Back"></Button>
		</StackPanel>
	</Grid>
</UserControl>
